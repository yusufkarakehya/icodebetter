version: 2
jobs:
  build:
    docker:
      - image: circleci/openjdk:8-jdk
    steps:
      - checkout
      - run: mvn clean compile jib:build -Djib.to.auth.username=icodebetter -Djib.to.auth.password=${ICB_HUB_PASSWORD}
